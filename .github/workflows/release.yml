name: Release

on:
    push:
        tags: ['v*.*.*']

jobs:
    publish:
        runs-on: ubuntu-latest
        permissions:
            contents: write # create the GitHub Release
            id-token: write # required for npm provenance (OIDC)
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 20
                  cache: npm
                  registry-url: https://registry.npmjs.org
            # Trusted Publishing (OIDC) requires npm >= 11.5.1; setup-node ships an older npm.
            - run: npm install -g npm@latest
            - run: npm ci
            - run: npm run typecheck
            - run: npm run lint
            - run: npm run test
            # No NODE_AUTH_TOKEN: auth happens via OIDC (Trusted Publisher on npmjs.com).
            # Provenance is generated automatically when publishing through a trusted publisher.
            - run: npm publish --access public

            - name: Extract release notes from CHANGELOG.md
              id: changelog
              env:
                  VERSION: ${{ github.ref_name }}
              run: |
                  # Lignes entre "## [x.y.z]" et le prochain titre "## [".
                  NOTES=$(awk -v v="${VERSION#v}" '
                      $0 ~ "^## \\[" v "\\]" {capture=1; next}
                      capture && /^## \[/ {exit}
                      capture {print}
                  ' CHANGELOG.md)
                  {
                      echo "notes<<EOF"
                      echo "$NOTES"
                      echo "EOF"
                  } >> "$GITHUB_OUTPUT"

            - name: Create GitHub Release
              uses: softprops/action-gh-release@v2
              with:
                  body: ${{ steps.changelog.outputs.notes }}
                  draft: false
                  prerelease: ${{ contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha') }}
